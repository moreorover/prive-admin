name: prive-admin-tanstack

services:
  postgres:
    image: postgres
    container_name: prive-admin-tanstack-postgres
    environment:
      POSTGRES_DB: prive-admin-tanstack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - prive-admin-tanstack_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  prive-admin-tanstack_postgres_data:
